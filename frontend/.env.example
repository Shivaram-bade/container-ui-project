# Keep this empty when the frontend uses the built-in /api proxy.
VITE_API_BASE_URL=

# Docker Compose provides this automatically. Override only for standalone development.
VITE_API_PROXY_TARGET=http://backend:8000
